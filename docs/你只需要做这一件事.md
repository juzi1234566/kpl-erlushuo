# 你只需要做这一件事（密钥）

代码、GitHub 仓库、DeepSeek 接入、同步脚本我已经弄好了。  
**只有 Supabase 的 3 个字符串必须从你的控制台复制**（我登录不了你的网页）。

## 1. 打开 Supabase

Project Settings → API，复制：

1. **Project URL**（形如 `https://xxxx.supabase.co`）
2. **anon public** key  
3. **service_role** key（保密，只给管线用）

## 2. 在项目根目录 PowerShell 执行

把下面三个替换成你的真值：

```powershell
cd C:\Users\18413\Desktop\kpl-meme
.\scripts\fill_supabase_keys.ps1 `
  -Url "https://你的项目.supabase.co" `
  -AnonKey "eyJ...anon..." `
  -ServiceRoleKey "eyJ...service_role..."
```

## 3. 告诉我「Key 填好了」

我会立刻帮你：

- 同步赛程 + 梗词条进库  
- 真 DeepSeek 生梗验收  
- Vercel 部署（Root Directory = `web`）

---

如果 Table Editor 里还没有表，在 SQL Editor 粘贴执行：

`supabase/migrations/0001_init.sql`
